#include "borrower.h"

int login_Borrower()
{

}

void Borrower_menu()
{//登录后的菜单
    /*
    char *p_name;
    int acc,swi;
    struct account_Admin login_a=login_Admin();
    p_name=login_a.name;
    acc=login_a.account;
    if(p_name[0]!='\0')
    {
        printf("欢迎您，%s，请选择操作：\n",p_name);
        printf("1.借阅图书\n2.归还图书\n3.借阅记录查询\n4.续借图书\n");
        scanf("%d",&swi);
        cleanScr(6);
        switch(swi)
        {
            case(1):break;
            case(2):break;
            case(3):break;
            case(4):break;
        }
    }
    */
}
